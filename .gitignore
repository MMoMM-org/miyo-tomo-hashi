# IDE
.vscode
*.code-workspace
*.iml
.idea

# npm
node_modules

# Build output — uploaded to GitHub releases, not committed
main.js
build/

# Local test vault — user notes, audio recordings, Obsidian state
# Never committed; live data only on the developer's machine.
test/Hashi/

# Test coverage
coverage/

# Sourcemaps
*.map

# Obsidian
data.json

# macOS
.DS_Store

# Workspace scratch / inter-project message channels — local-only, not committed
temp/
_inbox/
_outbox/

# MCP config (may contain API keys)
.mcp.json

# Claude Code
.claude/

# Docker-based dev environment (deployed by ~/Kouzou/scripts/) — anchored to repo root
/claude-docker/
/claude-docker-home/
/begin-code.sh
/generated-images/
