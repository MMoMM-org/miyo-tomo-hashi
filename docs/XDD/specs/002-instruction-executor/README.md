# Specification: 002-instruction-executor

## Status

| Field | Value |
|-------|-------|
| **Created** | 2026-04-24 |
| **Current Phase** | Closed — implemented, released (v0.7.1), release gate met by live Tomo→Hashi walks (2026-06-03) |
| **Last Updated** | 2026-06-03 |

## Documents

| Document | Status | Notes |
|----------|--------|-------|
| research.md | draft | Agent-team research synthesis — 5 perspectives (Requirements, Technical, Security, Integration, UX) |
| requirements.md | draft | PRD v2.0 — 11 Must features; 95 acceptance criteria; 0 open questions; pivots `.json` to source-of-truth + adds inbox-batch + run-log file |
| solution.md | draft | SDD v1.1 — 10 ADRs (5 revised on 2026-04-25 simplification review: ajv now runtime not standalone; FsPromisesVaultFS dropped; HookContext = `{action, app, logger}` only; no runState; no HookVault facade); ports-and-adapters at vault edge (VaultFS + ObsidianVaultFS + FakeVaultFS); state-machine modal; reuses 001's `Store<T>` and plain-TS-DOM patterns |
| plan/ | ready | 6-phase TDD plan (31 tasks); `plan/README.md` + `plan/phase-1..6.md`; full PRD AC → verification traceability shipped in `plan/traceability.md` (97/97 ACs, 100% coverage) |

## Scope (from ADR-009 + architecture-06 + Tomo consumer contract)

Parse `_instructions.json` produced by Tomo (`instruction-render.py` v0.6.0+) and execute the deterministic vault operations it describes, via the Obsidian Plugin API, with user-authored Node-script pre/post hooks.

Covers:
- **Input:** `_instructions.json` in vault inbox; `md_peer` field drives companion `.md` lookup; schema version 1
- **8 action kinds** (deterministic, order-constrained):
  1. `create_moc` — move + rename rendered MOC file to Atlas/200 Maps/
  2. `move_note` — move + rename rendered atomic note to Atlas/202 Notes/
  3. `link_to_moc` — append bullet line inside MOC section (section-name resolution + in-set MOC fallback)
  4. `update_tracker` — set tracker field on daily note (`inline_field` / `callout_body` / `checkbox`)
  5. `update_log_entry` — add prose line to daily log (`after_last_line` / `before_first_line` / `at_time`)
  6. `update_log_link` — add wikilink line to daily log (same position semantics; format `- [[stem]]`, `HH:MM - ` prefix when `at_time`)
  7. `delete_source` — move source to Obsidian trash (never hard-delete)
  8. `skip` — informational no-op
- **Execution order:** create_moc → move_note → link_to_moc → daily updates (tracker, log_entry, log_link) → delete_source → skip. Within block, monotonic `I01` … `INN`. `link_to_moc` MUST NOT run before its `create_moc`.
- **Sync contract:** after each successful action, tick matching `- [ ] Applied` checkbox in `.md` peer (`I##` ↔ third-level heading `### I## — …`). On failure, leave unchecked and surface error. Respect pre-ticked boxes (user may have applied manually) — skip those.
- **Hooks:** Node scripts in `.tomo-hashi/hooks/` (user-configurable). Pre/post per operation. Full plugin privilege — same trust model as Templater. Motivating case: `after-move.js` rewriting aliases across linking notes.
- **Tri-state execution mode:** `Confirm before run` (default, user clicks Execute) / `Auto-run with preview` (modal opens, execution starts immediately, user can Cancel) / `Silent` (no modal, Notice on completion). UX affordance, not an approval gate — approval is upstream in Tomo's review step.
- **Idempotency:** per-action rules documented in Tomo consumer contract §Action kinds — re-apply = no-op where states match; inconsistent state (both source and destination present) surfaces error rather than overwriting.
- **Partial-resume:** if N of M actions already ticked, skip them; re-run continues from first unticked.

Explicitly NOT in 002:
- Audit journal (see ADR-009 §6.3 — skipped by design, no consumer without rollback)
- Rollback / undo (file history, git, and user backups are recovery paths)
- LLM-driven section insertion for MOC sub-structure (backlog F-30 in Tomo — v0.1 appends at end of matched callout or falls back to first editable callout)
- Cross-vault operations

## Decisions Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-24 | Reset stale "blocked by 001" status + "Depends on 001" context | 2026-04-24 brainstorm pivot (logged in 001 decisions) made 002 standalone — no shared session identity or error channel with 001. README now reflects the pivot. |
| 2026-04-24 | Begin xdd workflow with PRD phase | Scope is outlined but acceptance criteria, hook API surface, partial-resume UX, and error reporting channel are still open questions from Kokoro's 2026-04-23 onboarding handoff — WHAT-level questions that need settling before design. |
| 2026-04-24 | Run xdd in Agent Team mode | Multiple design axes with potential tension (JSON schema strictness vs error UX; hook trust model vs simplicity; idempotency vs partial-resume progress reporting; Obsidian API surface for file moves and section editing). Persistent researcher perspectives outperform fire-and-forget agents here. |
| 2026-04-24 | Research phase complete — see `research.md` | 5 perspectives returned with strong alignment on core execution model (ports-and-adapters, ajv standalone, `app.fileManager.renameFile` for link preservation, `vault.process` for atomic writes). Conflicts narrowed to UX details (error channel location, popover extension) and integration policy (Periodic Notes fallback, peer-missing behaviour) — all surfaced for PRD. |
| 2026-04-24 | PRD v1.0 drafted with 0 clarification markers; all 8 research-level open questions pre-settled to recommended defaults | Defaults adopted: halt-on-dependency (yes), peer `## Errors` block (in-peer, not separate file), no status-bar popover extension in 002, palette command name "Execute instructions document", Periodic Notes as Should-Have fallback, example hook shipped, first-run disclosure modal + kill-switch for hooks, virtualization deferred to SDD. Any of these can still be redirected during review. |
| 2026-04-25 | **PRD v2.0 — major revision after user comments on v1.0.** Pivots `.json` to sole source of truth for applied-state (Tomo adds optional `applied: false` per action; Hashi flips to `true` on success). Adds inbox-batch invocation (one merged preview when no instruction file is active). Replaces in-peer `## Errors` block with per-run `tomo-hashi-run-log_<ts>.md` files (retention setting: always-keep / only-after-failed). Renames tri-state to *Confirm before run / Auto-run with preview / Silent*. Modal buttons standardised to **Execute / Cancel / Close**. Adds 橋 status-bar indicator as MUST. Drops sha256 hook disclosure (kept *enabled / disabled / ask* + kill-switch). Drops Periodic Notes / daily-notes resolution (Tomo emits absolute paths). Drops example hook on first run. Strengthens "never" markers on cross-vault, remote sources, hook sandboxing. Hooks dir now configurable. Adds debug-logging setting (per-hook detail behind it). | User revisions on requirements.md v1.0 inline; full revision answer round in conversation 2026-04-25. |
| 2026-04-25 | Outbound handoff to Tomo created at PRD exit (HIGH PRIORITY) | `_outbox/for-tomo/2026-04-25_hashi-to-tomo_applied-field.md` — request Tomo emit `applied: false` per action in `_instructions.json`. Schema stays v1 (additive optional field). Hashi v0.1 release gate requires the round-trip working. |
| 2026-04-25 | Tomo handoff returned **done** — `applied:false` shipped in Tomo v0.7.0 | `build_actions()` in `tomo/scripts/instruction-render.py` now stamps `applied:false` on every action; shared `$defs/applied_field` added to `tomo/schemas/instructions.schema.json` across all 8 variants; round-trip test asserts it; branch `feat/applied-field-instructions`, commit `f3ad49d`. Hashi can vendor the updated schema directly during Phase 1 of the plan; CON-9 in the SDD is no longer a blocker. |
| 2026-04-25 | SDD v1.0 — 10 ADRs confirmed in one batched round | 9 ADRs confirmed as proposed (ajv standalone codegen, vendored schema, createRequire+cache-evict hooks, pure handler functions, state-machine modal, vault.process for JSON applied-flag, Markdown per-run log file, vitest split, hook context with runState). ADR-6 revised: 橋 status bar uses idle/green/red color states only — no pulse animation. ADR-9 augmented: manual QA against `../temp/Privat-Test` documented as a release-gate checklist item. |
| 2026-04-25 | Plan v1.0 — 6 phases, 31 tasks, 4 parallel opportunities (T3.1, T5.1, T5.2, T5.3) | Phase 1 Foundation (vendor schema, types, settings UI, path safety, mock) → Phase 2 Vault Boundary & Schema (port + 3 adapters + validator) → Phase 3 Action Handlers (8 pure handlers + 2 helpers; T3.1 parallelizable) → Phase 4 Orchestrator/Hooks/Run Log (planner, applied-writer, peer-sync, run log, hook runner, executor) → Phase 5 UI Surfaces (modal, status bar, hook disclosure; 3 parallel) → Phase 6 Wire-up + live e2e + manual QA + traceability. Inherits `Store<T>` from 001 with on-demand extraction fallback. |
| 2026-04-25 | 4-perspective validation pass (Completeness / Consistency / Alignment / Coverage) — assessment **Critical** at first pass; **Excellent** after fixes | Findings: (HIGH) PRD F10 still described pulse animation in 5 places after ADR-6 was revised; (HIGH) PRD claimed 73 ACs but actual count is 95. (MEDIUM) Spec README scope blurb used old execution-mode names; SDD `minAppVersion` cited 1.5.7 but manifest is 1.5.0; SDD missed type definitions for `Diagnostic`, `ValidationFailure`, `ResolvedSource`, `Clock`, `Readable<T>`; PRD F5 edge-case self-contradicted F5 AC; PRD F11 missed rollback ACs for radio/toggle settings; F7 missed run-log-write-failure AC; 3 plan tasks missed test rows (F6 disabled-Execute, F10 ARIA live region, F11 per-hook ask non-persistence); plan parallelTasks counted 6 vs literal 4. (LOW) hooksDir trailing slash drift; broken `[ref: SDD/...]` strings; F10 tooltip lacked third "error" variant; outbound-handoff prose described handoff as pending though it returned done. **All findings fixed in this pass.** |
| 2026-04-25 | Spec 002 readiness = HIGH. Ready for implementation. | All 11 spec files present (README, research, requirements, solution, plan/README, plan/phase-1..6). 0 open questions. All 10 ADRs confirmed. Tomo `applied:false` handoff returned done in v0.7.0. PRD F1–F11 traceable to SDD components and plan tasks. Validation findings closed. |
| 2026-04-25 | Multi-batch review pass: security re-triage, drift, simplification, testing, compatibility | Five review batches landed: (1) Security — dropped sha-disclosure / lastTrustedAppId / TOCTOU / Tomo identity attestation as Won't Have (no named threat actor); kept H16 Docker-socket pinning, H17 fs.realpath, H19 xterm.js OSC 8/52, M15/M16 logging hygiene. (2) Drift — minAppVersion aligned at 1.5.0; AC counts reconciled; Svelte residue swept; SettingsTab path pinned. (3) Simplification — 6 ADRs revised: ADR-1 v2 runtime ajv (was standalone codegen); ADR-4 v2 drop runState; ADR-9 v2 drop FsPromisesVaultFS (manual QA is integration gate); ADR-10 v2 HookContext = `{action, app, logger}`; ConnectionError 7→4; HandlerOutcome collapsed into ActionOutcome; ValidationFailure → `{ok, message: string}`; runState/HookVault/disableAllHooks/derived/connectionStoreWrite all dropped. (4) Testing — RED-GREEN-REFACTOR canonical task shape; T4.4.0 hook fixture set; F9 fs.realpath EARS; edge-case→tests matrix; manual-QA in T6.4 is now load-bearing per ADR-9 v2. (5) Compatibility — schema forward-compat policy added to PRD Constraints; HookContext stability policy added to ADR-10; two new outbound handoffs in `_outbox/for-tomo/` (`schema-changelog-discipline.md`, `path-emission-contract.md`). |
| 2026-04-29 | F7 run-log fingerprint dropped — log records free-text content fields verbatim. PRD v2.1, SDD v1.1. | The F7 AC originally required an 8-char sha256 fingerprint of `update_tracker.value` / `update_log_entry.line` etc. on the rationale that log files travel with vault sync. The run log lives in the same `<tomo-inbox>/` folder as the source `_instructions.json` files which already contain those exact values in plain text. Hashing one file while its uncrypted source sits beside it adds ceremony without protecting anything. Aligned with the existing "no crypto ceremony without a named threat actor" project stance (cf. dropped sha-disclosure, lastTrustedAppId, etc.). PRD F7 AC and SDD F7 EARS revised in this pass. |
| 2026-04-29 | SDD CON-7 bundle ceiling raised 1000 KB → 1200 KB. | T6.2 wiring of 002 surfaces (executor + planner + 8 handlers + run log + hook runner + 3 UI surfaces + ajv runtime) brought build/main.js to ~1105 KB; the 1000 KB target was set before 002's full surface area was integrated. Follow-up bundle audit task tracked: investigate ajv code-gen per ADR-1 v1, lazy-load xterm (only used by 001's TomoChat). SDD bumped to v1.2. |
| 2026-04-29 | **Phase 6 close** — Spec 002 implementation complete. | 31 plan tasks across 6 phases shipped; 705 unit tests passing across 51 test files; lint + build clean; manual QA checklist authored (`plan/manual-qa-checklist.md`); traceability matrix complete (`plan/traceability.md`, 97 ACs at 100% coverage). Outstanding: real-Obsidian walk by user against `../temp/Privat-Test`. Spec moves from `draft` → `ready` for v0.1 release once manual walk passes. Follow-up: bundle audit (CON-7 raised 1000 → 1200 KB at T6.2; v0.1 polish opportunity). |
| 2026-04-29 | **npm-audit triage closed — option (c) accepted: findings stand as documented noise.** | Kouzou returned the triage decision (`_inbox/from-kouzou/2026-04-28_kouzou-to-hashi_npm-audit-triage-decision.md`): no Docker image change, no MiYo-wide `uuid >= 14` overrides policy. Rationale: (1) `picomatch`/`brace-expansion` resolve through Hashi's own lockfile (`semantic-release@24 → @semantic-release/npm@12 → npm@10.9.8`), not Kouzou's image — bumping the image npm would not change Hashi's audit output; `semantic-release` runs at release time only and is never bundled into `main.js`. (2) The `dockerode → uuid` advisory (GHSA-w5hq-g745-h8pq) requires a caller-supplied `buf` argument that dockerode never passes; forcing `uuid >= 14` via `overrides` jumps four majors past dockerode's tested range and trades a non-exploitable advisory for real compatibility risk. **All four findings are non-exploitable in Hashi's plugin runtime.** v0.1 release-readiness: findings are recorded here as known noise; passively watch dockerode upstream for a uuid-14 bump (routine `npm update dockerode` closes the runtime-reaching finding when it lands — no automation, no block). |
| 2026-04-29 | **Tomo format-spec response received — contract deltas logged as post-Phase-6 follow-up.** | Tomo closed Ask 1+2 of the 2026-04-29 format-spec handoff (`_inbox/from-tomo/2026-04-29_tomo-to-hashi_format-spec-and-section-name-response.md`; Tomo commits `23fab95` + `794f219` + `a7cf1dd`). Most contract is confirmed as Hashi suspected. **Contract changes that supersede shipped Hashi behavior** (each requires a follow-up TDD revision; not landed in this branch): (1) **PRD F4 `update_tracker` overwrite semantics** — flip from "different value → fail" to "Tomo's intent wins → set the value" (manual-qa-checklist row `F4.update_tracker.differs` line 130 currently encodes the old semantics). (2) **`update_log_entry.at_time` line shape** — `- HH:MM: <content>` (Hashi composes; Tomo emits bare prose + structured `time` field). Old prose contract `HH:MM - <content>` is retired. (3) **`update_log_link.at_time` line shape** — `- HH:MM: [[stem]]`, aligned with `update_log_entry`. Old `HH:MM - - [[stem]]` shape (manual-qa-checklist row `F4.update_log_link.insert` line 132) is retired. (4) **`update_tracker` matcher** — must recognise three Dataview positions (line-anchored `<f>:: …`, bracketed `[<f>:: …]`, parenthesized `(<f>:: …)`); insertion stays line-anchored only. `inline_field` matcher uses token-anywhere semantics, not `startsWith(field + "::")`. (5) **`callout_body` syntax** — double-colon only (`> field::`); single-colon is not a Tomo-supported variant. (6) **Multi-word field names** require regex-escape or literal scanning. (7) **`link_to_moc.section_name` heading-targeting** is dead code in v0.1 (Tomo never emits heading-target shapes); keep as documented future-extension, do not exercise in v0.1 QA. **Profile-driven format** (e.g. non-miyo `daily_log.entry_time_format`) is post-MVP on Tomo's side; Hashi hardcodes `- HH:MM: ` for miyo profile. **Spec-002 documents that need revision in the follow-up branch:** PRD F4 (update_tracker overwrite + line-shape ACs); SDD section-locator + matcher contract; `plan/manual-qa-checklist.md` rows F4.update_tracker.differs + F4.update_log_link.insert; production handlers `handleUpdateTracker` / `handleUpdateLogEntry` / `handleUpdateLogLink` and matcher helpers; corresponding unit tests. **Note:** items (1)–(3) are AC-level revisions; (4)–(6) are matcher behavior the existing handlers may already cover correctly — needs verification before declaring scope. Tomo's Ask 3 (synthetic-source emission round) is deferred to a separate Tomo session per user direction. |
| 2026-04-29 | **Tomo format-spec contract deltas LANDED — items (1)–(7) closed in this branch.** | Same-day follow-up to the entry above. All seven items addressed in `feat/spec-002-instruction-executor`: **Specs revised** — PRD F4 ACs (`update_tracker` overwrite flips to "Tomo's intent wins"; `update_log_entry` + `update_log_link` line shapes documented as `- <content>` for after/before and `- HH:MM: <content>` / `- HH:MM: [[stem]]` for at_time); manual-qa-checklist rows `F4.update_tracker.differs` flipped to "applied" + 4 new rows added (`inline-bracketed`, `inline-paren`, `inline-bullet`, `multi-word`); `F4.update_log_link.insert` row updated to new line shape; `traceability.md` F4.7/F4.8/F4.9 brief summaries refreshed and now name 7 manual-QA AC IDs for F4.7. **Code revised** — `src/actions/updateTracker.ts` `handleInlineField` now runs a 3-priority matcher (line-anchored with bullet/indent tolerance > inline-bracketed > inline-parenthesized) using `RegExp` with `escapeRegExp` for multi-word field names; both `handleInlineField` and `handleCalloutBody` now overwrite-on-differ via `vault.process`; `src/actions/updateLogEntry.ts` line composition now `- ${content}` / `- ${time}: ${content}`; `src/actions/updateLogLink.ts` line composition now `- [[stem]]` / `- ${time}: [[stem]]`; `src/actions/logPosition.ts` `TIME_PREFIX` regex updated to `/^-\s+(\d{2}:\d{2}):/` to match the new shape. **Tests revised** — 4 test files updated (updateTracker, updateLogEntry, updateLogLink, logPosition); 7 new tests added (bullet-prefix line, inline-bracketed, inline-paren, line-anchored-priority, multi-word inline, multi-word callout, legacy-shape-fallback). **Verification** — full suite 712 passing (was 705; +7 new); lint clean; build clean (1132 KB, under the 1200 KB CON-7 ceiling). Heading-targeting `link_to_moc` (item 7) is confirmation-only — no code change; manual-qa-checklist already does not exercise it in v0.1. |
| 2026-04-30 | **First end-to-end manual QA walk + section_locator bug fix.** | First manual walk against `test/Hashi` post-Phase-6 with the contract-delta + path-shape contract landed (run log archived at `_walks/2026-04-30T1131_run-log.md`). Run completed 39/39 actions (36 applied + 3 skipped-already + 0 failed). **Confirmed working**: `update_log_entry`/`update_log_link` with new `- HH:MM:` line shape; `delete_source` on `.m4a` audio with `:` in filename (Issue 3 from 2026-04-30 path-shape handoff — Obsidian's `vault.trash()` reaches colon-bearing media on macOS, no Kado/hook fallback needed); schema validation; partial-resume idempotency. **Bug found + fixed**: `src/actions/sectionLocator.ts` callout-match regex stripped the `[!type]` prefix when extracting the title; Tomo's `link_to_moc.section_name` is emitted in prefixed shape (e.g., `"[!blocks] Key Concepts"`); the comparison with the prefix-stripped extracted title always failed; `linkToMoc` then fell back to "first editable callout" (`[!connect] Your way around` in nearly every MOC). Result: all 13 link_to_moc actions landed in the wrong callout (10 wrote, 3 idempotent due to pre-existing duplicates from earlier walks). Fix: `locateSection` now parses `desiredSectionName` for an optional `[!type]` prefix and matches both type AND title (case-insensitive); plain-title path preserved for backward compat; heading lookup is skipped when prefix is present (callout-specific intent). +5 new RED tests for the prefix path, all GREEN; full suite 717/717 passing (was 712); lint + build clean. **Two outbound handoffs filed** (Tomo): (a) `move_note` should implicitly delete `origin_inbox_item` unless user marks "keep" — currently 11 origins remain in inbox post-walk because Tomo emits no paired `delete_source`; (b) `link_to_moc` needs an explicit placement mode (`inside` callout vs `after` section) — current schema flattens to "inside" only. **Open follow-ups**: empty summary modal at run end (Hashi UI bug — investigate); pre-existing duplicates in MOC `[!connect]` blocks polluted idempotency check (cleanup before next walk + new Archive.zip baseline). |
| 2026-04-30 | **Tomo move-note origin-delete response — Option A.1 confirmed, no Hashi-side changes.** | Response to 2026-04-30 outbound (`_archive/outbox/2026-04/2026-04-30_hashi-to-tomo_move-note-implicit-origin-delete.md`): Tomo will emit paired `delete_source` actions for every `move_note` whose origin should be cleaned up, gated by a new "Keep origin" checkbox in the suggestions doc (default unchecked → paired delete; user opts out by checking → no paired delete). Pure Tomo-side change in `_build_delete_source_actions` (new third emission source: every move_note whose confirmed item has `keep_origin: false` AND non-null `origin_inbox_item`). **Hashi handler unchanged** — the new `delete_source` actions use the same shape `deleteSource.ts` already handles. User-visible change after Tomo ships: 11 origins (Asahikawa.md, Furano.md, etc.) disappear from inbox by default after a walk that contains their corresponding `move_note` actions. The 2026-04-30 path-shape "peer files are independent actions" contract still holds for audio + transcript pairs (independent upstream artifacts) — the `move_note` origin pairing is the carve-out for Tomo-mediated input → output transformations. Tomo-side branch in-flight; Marcus regenerates test inbox post-merge before next walk. Inbox handoff marked done. |
| 2026-05-01 | **Tomo link-placement-mode contract LANDED — `link_to_moc` reworked + new `add_relationship` action.** | Implementation of the 2026-04-30 Tomo response on `feat/spec-002-instruction-executor`. Tomo emission `2026-05-01_0712` already uses the new shape; current Hashi code would have failed validation + runtime. **Schema** — vendored `src/schema/instructions.schema.json` updated from Tomo's authoritative copy: `link_to_moc.section_name` removed; new required `anchor: {type: "callout"\|"heading"\|"line", value: string\|null}` + `placement: "inside"\|"after"`; new `$defs/anchor`; new `$defs/add_relationship`. **Types** — `LinkToMocAction` reshaped (drop `section_name`, add `anchor` + `placement`); new `Anchor` interface; new `AddRelationshipAction`; `Action` union grew to 9 variants; `ActionKind` derived enum mirrors. **Code** — new `src/actions/anchorResolver.ts` (resolves all 3 anchor types; returns insertInside + insertAfter line indices; null if value is null or no match); `src/actions/linkToMoc.ts` rewritten to use anchorResolver + placement modes (no fallback — anchor-not-found is hard fail; placement: inside on non-callout is hard fail); new `src/actions/addRelationship.ts` (marker-based first-match locator, whole-line replacement, normalises `> ` callout prefix to canonical single-space form, idempotent on equality); `src/actions/index.ts` HANDLERS dispatch updated; `src/executor/planner.ts` KIND_ORDER inserts `add_relationship` between `link_to_moc` and `update_tracker` (relationship-marker rewrites belong on the freshly-linked MOC). **Tests** — new `test/unit/actions/anchorResolver.test.ts` (10 tests); new `test/unit/actions/addRelationship.test.ts` (7 tests); `test/unit/actions/linkToMoc.test.ts` rewritten end-to-end (12 tests covering anchor × placement matrix, no-fallback, defensive fail on inside-non-callout). Existing fixtures across InstructionExecutor.test.ts, planner.test.ts, HookRunner.test.ts, schema/types.test.ts migrated from `section_name` to `anchor` + `placement` shape. **Verification** — full suite 740 passing (was 717; +23 new across anchorResolver + addRelationship + linkToMoc rewrite); lint clean; build clean (1132 KB); tsc strict clean. **Spec docs** — PRD F4 link_to_moc AC rewritten + new F4 add_relationship AC; canonical-order line updated to 9-kind sequence; manual-qa-checklist 14 new rows (8 link_to_moc + 6 add_relationship); traceability F4.6 updated + new F4.6b. **Open**: heading-anchor and line-anchor are now supported by Hashi but Tomo's emission only uses callout anchors so far — heading + line paths exercised via unit tests; no real-Obsidian QA data yet. The implementation honours Tomo's "no back-compat reader" call — old `section_name` fixtures rejected at TS compile time. |
| 2026-04-30 | **Tomo link-placement-mode response — substantial contract broadening; significant Hashi-side rework required.** | Response to 2026-04-30 outbound (`_archive/outbox/2026-04/2026-04-30_hashi-to-tomo_link-placement-mode.md`). Tomo broadens the original `placement: "inside"\|"after"` proposal into a two-action contract: **(1) `link_to_moc` reworked** — `section_name` field DROPPED; new required `anchor: {type: "callout"\|"heading"\|"line", value: string}` object + new required `placement: "inside"\|"after"` field. Three anchor types (callout existing, heading + line NEW). `inside` placement valid only for `callout` anchor (Hashi may runtime-error or treat as `after` on mismatch — Tomo won't emit). Default `placement` flips from "inside" to **"after"**; Tomo emits `placement` explicitly on every action (no Hashi-side defaulting). `line_to_add` is now pre-formatted by Tomo (bullet style, prefix conventions live upstream); Hashi writes verbatim with `> ` prefix iff `inside` + callout. **(2) `add_relationship` NEW action** — for navigation links written as Dataview inline fields (`up::`, `related::`). Shape: `{action: "add_relationship", target_moc_path, marker: "up::"\|"related::", line: "<verbatim line>"}`. Hashi behaviour: locate line whose stripped content starts with `marker` (after `> ` strip), replace whole line with `line` verbatim (preserve leading `> ` callout prefix). Hard fail on missing marker (no fallback). Multi-link aggregation for `related::` done Tomo-side before emission. Schema stays at v1 per migration call (Marcus regenerates test inbox before re-walking; no back-compat reader on Hashi). **Hashi-side scope (post-Phase-6 amendment, separate branch):** schema types update (drop `section_name`, add `anchor` + `placement` to `LinkToMocAction`; add new `AddRelationshipAction`); rewrite `linkToMoc.ts` handler; extend `sectionLocator.ts` for heading + line anchor types (or split into anchor-resolver); new `addRelationship.ts` handler; PRD F4 amendment + new F4 sub-spec for `add_relationship`; SDD update; ~30 new tests covering anchor matrix × placement modes; manual-qa-checklist + traceability rows. **Implementation gated on Tomo emission landing first** — current `2026-04-30_1351_instructions.json` still uses old shape; new shape will arrive on Tomo's `feat/link-anchor-and-relationship-actions` (or similar) branch. Inbox handoff marked done; implementation tracked as a separate work item. |
| 2026-05-27 | **Real-data + hook-hardening wave (PR #10) — schema drift + hook lifecycle fixes from a full Tomo note-set walk.** | Second testing wave, this time on a full Tomo note set with a user-authored `.cjs` alias-rewrite hook (the `docs/hooks.md` "Rewrite aliases after move" example). Fixes, each shipped with regression tests: **(1)** `cf855d8` schema `create_moc.supporting_items` widened to string\|array — Tomo's MOC-proposal-parser emits an array while the suggestion-parser still emits a comma-separated string (ref `_inbox/from-tomo/2026-05-27_tomo-to-hashi_schema-drift-018.md`). **(2)** `895c0ac` strip `tomo:` lifecycle frontmatter (`doc_type`/`state`/`run_id`/`updated_at`) after `move_note` + `create_moc` via `vault.process` (`stripTomoFrontmatter.test.ts`, +88). **(3)** `373fde0` `FsHookLoader` rejects `.js`, accepts `.cjs` only — Electron treats `.js` as ESM so `module.exports` is a no-op (the alias-hook discovery). **(4)** `0d376b3` close the disclosure modal after the user decides (was leaving a zombie modal on screen). **(5)** `4e43d4b` `HookRunner.preApprove()` runs all ask-mode disclosures upfront so no modal interrupts mid-execution (`HookRunner.test.ts`, +104). **(6)** `a58acc9`/`3fd9bc2` documented the alias-rewrite hook as the real-world worked example. |
| 2026-06-03 | **Spec 002 CLOSED for v0.1 — release gate met by live Tomo→Hashi walks, owner-accepted.** | The synthetic 177-row `plan/manual-qa-checklist.md` was deliberately **not** walked row-by-row; it tests Hashi against invented data, whereas every real v0.1 defect was Tomo↔Hashi drift (schema-drift-018; the 2026-04-29/05-01 format-spec deltas; the `link_to_moc` anchor-model rework) — drift from-scratch fixtures could not have surfaced. The effective gate was the two real-vault testing waves logged above (2026-04-30 walk, evidence `_walks/2026-04-30T1131_run-log.md`; 2026-05-27 full-note-set + alias-hook wave), which exercised all 8 action kinds plus `add_relationship`, schema validation against drifting real output, callout/section resolution, run-log generation, and the full `.cjs` hook pipeline with disclosure flow. All defects found were fixed and shipped **with regression tests** (constitution L2 satisfied). **Accepted gap:** path-safety deny-list rejection (F9), `schema_version: 2` rejection (F2), mid-run cancellation, silent-mode runs, and missing-`.md`-peer runs were not present in the real note sets and remain unit-tested only — acceptable for v0.1. `manual-qa-checklist.md` status flipped `pending → superseded-by-live-walks`. **Future option (not a v0.1 blocker):** a Tomo-emitted fixture set ("tell Tomo what to emit") if repeatable automated integration coverage is ever wanted; generating fixtures from scratch is explicitly rejected as it would never surface the integration drift that actually bites. |
| 2026-04-30 | **Tomo audio-peer-path-emission response received + closed; three new contract items captured Hashi-side.** | Same-day round-trip to the 2026-04-29 outbound (`_archive/outbox/2026-04/2026-04-29_hashi-to-tomo_audio-peer-path-emission.md`). Tomo shipped `feat/audio-path-emission@2815f46` — new `_ensure_md_extension` helper at the three emitter sites (`_build_delete_source_actions`, `_build_skip_actions`, `_build_move_note_actions`) using a known-extension allowlist (`md`, `m4a`, `mp3`, `wav`, `mp4`, `pdf`, `png`, …). Root cause was `_stem(src)` in the `.md` peer renderer stripping a trailing `.md` only — masking the underlying JSON bug that always appended `.md` regardless of the wikilink target's existing extension. Regression tests B3/B4/B5 added in `test-008-phase1.py`. **Three new contract items formalised in Tomo's `docs/instructions-json.md` § Path Shape Contract** and now captured Hashi-side: (1) **Path-shape rules** — `source_path` is byte-equal to the `.md` peer's wikilink target; Hashi consumes verbatim. (2) **Peer-independence** — media + transcript files with shared stems are emitted as separate actions reflecting separate user decisions; **Hashi MUST NOT infer peer deletion from shared stems / prefixes / heuristics** (the I31/I32/I36/I39 pattern from this round's QA setup, where audio-2 is deleted but transcript-2 is kept, is the canonical supported flow). (3) **Filename character handling** — forbidden-char files (recorder-named `.m4a` containing `:` on macOS) appear in actions with their literal on-disk names; Tomo's `sanitize_stem` is applied only at write-time for files Tomo creates; Hashi does NOT reverse-sanitise. **Hashi-side capture**: PRD F4 `delete_source` AC extended with all three clauses; `manual-qa-checklist.md` adds two new rows (`F4.delete_source.peer-independent` + `F4.delete_source.colon-bearing-media`); `traceability.md` F4.10 row updated to reference both new ACs. **Issue 3 (vault.trash on `:`-bearing media on macOS)** explicitly deferred to QA-walk observation — per F4 idempotency the executor returns `skipped-already` without aborting, so the run continues regardless. If `skipped-already` is the consistent outcome, follow-up handoff to Kado / hook author. The existing inline `[path corrected 2026-04-29 …]` patches in `test/Hashi/100 Inbox/2026-04-29_1541_instructions.json` stay until Tomo re-emits a fresh round. |

## Open Questions (from Kokoro onboarding handoff 2026-04-23)

1. **Hook API surface.** Minimum: current action payload, vault-writing helper, read helper, logger. Keep surface minimal in v0.1; users can reach Obsidian API directly via `app` if they know what they're doing.
2. **Partial-resume UX.** If 10 of 25 actions ticked, show progress summary ("15 of 25 remaining") in preview modal before executing? Kokoro leans yes.
3. **Error reporting channel.** Must align with 001's decision — per-action failures surface where?

## Context

After the 2026-04-24 brainstorm pivot (logged in spec 001 decisions), 002 is **standalone**:
- No dependency on 001 for container identity — the executor runs against `_instructions.json` files in the vault, regardless of whether a Tomo container is currently attached.
- No dependency on 001 for an error channel — 002 surfaces its own per-action errors (channel TBD in PRD/SDD).
- No shared session state with 001.

Both features must land for v0.1; they can proceed in parallel. Implementation (plan/) should land into the same plugin skeleton 001 builds, but 002 has no runtime coupling to 001's connection service.

## References

- ADR-009 §3 Instruction-Set Execution Model, §6.1 Hook Delivery, §6.2 Preview Modal
- Architecture 06 §6 Instruction-Set Execution Model
- Tomo consumer contract: `/Volumes/Moon/Coding/MiYo/Tomo/docs/instructions-json.md`
- Tomo JSON Schema: `/Volumes/Moon/Coding/MiYo/Tomo/tomo/schemas/instructions.schema.json`
- Onboarding handoff: `_inbox/from-kokoro/2026-04-23_kokoro-to-hashi_onboarding-charter-contract-and-v01-scope.md`
